{
  "name": "megafon-intraservice",
  "version": "3.0.0",
  "private": true,
  "description": "МегаФон ВАТС → Node.js → PostgreSQL → IntraService",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "check": "node --check src/server.js",
    "migrate": "node scripts/migrate.js"
  },
  "engines": {
    "node": ">=24"
  },
  "dependencies": {
    "pg": "^8.16.3"
  }
}
